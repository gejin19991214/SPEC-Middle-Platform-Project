import React from "react";
import Typed from "react-typed";

const Home = () => {
  return (
    <div className="home">
      <Typed strings={["SPEC-Middle-Platform-Project"]} typeSpeed={40} />
    </div>
  );
};

export default Home;
